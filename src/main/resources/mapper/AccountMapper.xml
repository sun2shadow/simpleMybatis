<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shadow.foretaste.AccountDao">
    <insert id="insertAccount" parameterType="com.shadow.foretaste.entity.Account" useGeneratedKeys="true" keyProperty="account_id">
        insert into account (user_id, money) values (#{userId}, #{money})
    </insert>
</mapper>
